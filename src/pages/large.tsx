import { NextPage } from "next";
import Link from "next/link";

interface Props {}

const Large: NextPage = ({}) => {
  return (
    <div className="flex flex-col p-8 items-center justify-center w-screen h-screen bg-slate-300">
      large
    </div>
  );
};
export default Large;
